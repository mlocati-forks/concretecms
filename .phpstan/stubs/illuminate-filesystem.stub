<?php

/*
 * Filesystem::get() is documented as returning a string, but it returns the result of file_get_contents(), which is
 * false when the file exists but it can't be read: this stub declares the actual type.
 * Filesystem::exists() returns the result of file_exists(), which depends on the state of the filesystem: this stub
 * marks it as impure, so that PHPStan doesn't reuse its result after a file has been created or deleted.
 */

namespace Illuminate\Filesystem;

class Filesystem
{
    /**
     * @phpstan-impure
     *
     * @param string $path
     *
     * @return bool
     */
    public function exists($path)
    {
    }

    /**
     * @param string $path
     * @param bool $lock
     *
     * @return string|false false if the file exists but it can't be read (an exception is thrown if it doesn't exist)
     */
    public function get($path, $lock = false)
    {
    }
}
