<?xml version="1.0" encoding="UTF-8"?>
<schema
  xmlns="http://www.concrete5.org/doctrine-xml/0.5"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.concrete5.org/doctrine-xml/0.5 https://concretecms.github.io/doctrine-xml/doctrine-xml-0.5.xsd">

  <table name="btSearch">
    <field name="bID" type="integer" comment="The ID of the block">
      <unsigned/>
      <key/>
    </field>
    <field name="title" type="string" size="255" comment="The text displayed above the search box"/>
    <field name="buttonText" type="string" size="128" comment="The text of the button that starts the search"/>
    <field name="baseSearchPath" type="string" size="255" comment="The path of the page whose descendants are searched (empty: the whole site)"/>
    <field name="search_all" type="boolean" comment="true: every site of the installation is searched, and baseSearchPath is ignored; false or NULL: only the current site is searched">
      <default value="0"/>
    </field>
    <field name="allow_user_options" type="boolean" comment="true: the visitors can choose whether to search every site of the installation; false or NULL: they can't">
      <default value="0"/>
    </field>
    <field name="postTo_cID" type="integer" comment="The ID of the page that displays the results (NULL: they are displayed in the page holding the block, or at resultsURL)">
      <unsigned/>
    </field>
    <field name="resultsURL" type="string" size="255" comment="The address the search is sent to (used if postTo_cID is NULL)"/>
  </table>

</schema>
