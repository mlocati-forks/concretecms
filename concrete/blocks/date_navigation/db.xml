<schema
  xmlns="http://www.concrete5.org/doctrine-xml/0.5"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.concrete5.org/doctrine-xml/0.5 https://concretecms.github.io/doctrine-xml/doctrine-xml-0.5.xsd">

  <table name="btDateNavigation">
    <field name="bID" type="integer" comment="The ID of the block">
      <unsigned/>
      <key/>
    </field>
    <field name="title" type="string" size="255" comment="The text displayed above the list of the dates">
      <default value=""/>
      <notnull/>
    </field>
    <field name="filterByParent" type="boolean" comment="true: the months come from the pages under cParentID; false or NULL: they come from the whole site">
      <default value="0"/>
    </field>
    <field name="redirectToResults" type="boolean" comment="true: the links that the block builds point to the cTargetID page; false or NULL: they point to the page holding the block">
      <default value="0"/>
    </field>
    <field name="cParentID" type="integer" comment="The ID of the page holding the pages the months come from (used if filterByParent is true)">
      <unsigned/>
      <default value="0"/>
      <notnull/>
    </field>
    <field name="cTargetID" type="integer" comment="The ID of the page that the links of the block point to, where a page list block filters its pages by the chosen month (used if redirectToResults is true)">
      <unsigned/>
      <default value="0"/>
      <notnull/>
    </field>
    <field name="ptID" type="smallint" comment="The ID of the page type that those pages must have (0 or NULL: any page type)">
      <unsigned/>
    </field>
    <field name="titleFormat" type="string" size="20" comment="The HTML element wrapping the title: h1, h2, h3, h4, h5, h6 or p">
        <default value="h5" />
		<notnull/>
    </field>
  </table>

</schema>
