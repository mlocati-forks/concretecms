<?xml version="1.0" encoding="UTF-8"?>
<schema
        xmlns="http://www.concrete5.org/doctrine-xml/0.5"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.concrete5.org/doctrine-xml/0.5 https://concretecms.github.io/doctrine-xml/doctrine-xml-0.5.xsd">

    <table name="btCalendarEvent">
        <field name="bID" type="integer" comment="The ID of the block">
            <unsigned/>
            <key/>
        </field>
        <field name="mode" type="string" size="1" comment="S: the event in eventID is displayed; P: the one held by the page attribute named by calendarEventAttributeKeyHandle; R: the occurrence passed through the URL">
            <default value="S"/>
            <notnull/>
        </field>
        <field name="calendarEventAttributeKeyHandle" type="string" size="255" comment="The handle of the page attribute holding the event to be displayed (used if mode is P)">
            <default value=""/>
            <notnull/>
        </field>
        <field name="calendarID" type="integer" size="10" comment="The ID of the calendar the event belongs to">
            <unsigned/>
            <default value="0"/>
            <notnull/>
        </field>
        <field name="eventID" type="integer" size="10" comment="The ID of the event to be displayed (used if mode is S)">
            <unsigned/>
            <default value="0"/>
            <notnull/>
        </field>
        <field name="displayEventAttributes" type="text" comment="A JSON array of the IDs of the event attributes to be displayed">
        </field>
        <field name="allowExport" type="boolean" comment="true: the visitors are offered a link to add the event to their own calendar; false or NULL: they are not">
            <default value="0"/>
        </field>
        <field name="enableLinkToPage" type="boolean" comment="true: the name of the event links to the page that the event is bound to, opening it at the occurrence being displayed; false or NULL: the name is not a link">
            <default value="0"/>
        </field>
        <field name="displayEventName" type="boolean" comment="true: the name of the event is displayed; false or NULL: it's left out">
            <default value="0"/>
        </field>
        <field name="displayEventDate" type="boolean" comment="true: the date of the event is displayed; false or NULL: it's left out">
            <default value="0"/>
        </field>
        <field name="displayEventDescription" type="boolean" comment="true: the description of the event is displayed; false or NULL: it's left out">
            <default value="0"/>
        </field>


    </table>
</schema>
