<?xml version="1.0" encoding="UTF-8"?>
<schema xmlns="http://www.concrete5.org/doctrine-xml/0.5" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.concrete5.org/doctrine-xml/0.5 https://concretecms.github.io/doctrine-xml/doctrine-xml-0.5.xsd">
    <table name="btGoogleMap">
        <field name="bID" type="integer" comment="The ID of the block">
            <unsigned/>
            <key/>
        </field>
        <field name="title" type="string" size="255" comment="The text displayed above the map" />
        <field name="location" type="string" size="255" comment="The address the map is centered on, as the visitors read it" />
        <field name="latitude" type="float" comment="The latitude the map is centered on, which Google Maps resolved the location to" />
        <field name="longitude" type="float" comment="The longitude the map is centered on, which Google Maps resolved the location to" />
        <field name="zoom" type="smallint" comment="How close the map is zoomed in, from 0 (the whole world) to 21 (a single building)" />
        <field name="width" type="string" size="8" comment="The width of the map, as a CSS length (for instance 100% or 400px)">
            <default value="100%" />
        </field>
        <field name="height" type="string" size="8" comment="The height of the map, as a CSS length (for instance 300px)">
            <default value="400px" />
        </field>
        <field name="scrollwheel" type="boolean" comment="true: the visitors can zoom the map with the wheel of the mouse; false: the wheel scrolls the page">
            <default value="1" />
            <notnull/>
        </field>
	    <field name="titleFormat" type="string" size="20" comment="The HTML element wrapping the text displayed above the map: h1, h2, h3, h4, h5, h6 or p">
	      <default value="h3" />
		  <notnull/>
	    </field>
    </table>
</schema>
